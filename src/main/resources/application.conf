ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.tournamentpilot.ApplicationKt.module ]
    }
}

database {
    url = "jdbc:mysql://localhost:3306/tournamentpilot?createDatabaseIfNotExist=true&serverTimezone=UTC"
    driver = "com.mysql.cj.jdbc.Driver"
    user = "root"
    password = "password"
    maxPoolSize = 10
}
